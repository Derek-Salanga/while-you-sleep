# Generated by npm. Formatting it is pointless churn on a file nobody edits
# by hand, and risks a CI failure the next time npm regenerates it.
package-lock.json

# Hand-wrapped prose. Prettier's only change to the markdown in this repo is
# *emphasis* -> _emphasis_, which fights the convention used consistently
# throughout CLAUDE.md and improves nothing. Formatting is enforced for code,
# where it settles real arguments, not for docs where it just churns.
*.md
